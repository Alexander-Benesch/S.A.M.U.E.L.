{
  "weights": {
    "test_pass_rate": 0.3,
    "syntax_valid": 0.2,
    "hallucination_free": 0.3,
    "scope_compliant": 0.2
  },
  "baseline": 0.8,
  "fail_fast_on": ["syntax_valid"],
  "_test_runner_doc": "test_cmd: command with {test} placeholder, e.g. pytest -q -k {test}. test_timeout: seconds (default 60). Wenn nicht gesetzt, Marker-Auto-Detection (pyproject.toml/package.json/go.mod/Cargo.toml/pom.xml).",
  "test_cmd": null,
  "test_cmd_fallback": null,
  "test_timeout": 60
}