# Handover-Doku — Stand 2026-05-06

Übergabe-Doku für die nächste Session. Ziel: ohne Lektüre der vorigen Chat-Historie sofort weiterarbeiten können.

## Repo-Kontext

- **Working dir:** `/home/alexanderbenesch/S.A.M.U.E.L.`
- **Gitea:** `Alexmistrator/S.A.M.U.E.L` auf `http://192.168.1.60:3001`
- **Gitea-Token:** in `/home/alexanderbenesch/gitea-agent/.env` (`GITEA_TOKEN`, `GITEA_URL`, `GITEA_REPO`)
- **Branch:** `main` — alle PRs werden direkt nach main gemerged
- **Tests:** `1353 passed, 1 skipped` als Baseline. Befehl: `.venv/bin/python -m pytest tests/ samuel/ -q`
- **Dashboard-Restart:**
  ```bash
  pkill -9 -f "samuel.*dashboard"
  find samuel -name __pycache__ -delete
  nohup .venv/bin/python -m samuel --self dashboard --host 0.0.0.0 --port 7777 > /tmp/samuel-dashboard.log 2>&1 & disown
  ```

## Was passierte am 2026-05-06

Eine Audit-Welle plus Folge-Implementierungen plus Hotfix-Kette plus Repo-Cleanup:

### Audit + Konsolidierung des Per-Provider-Prompt-Systems

| PR | Issue | Was |
|---|---|---|
| #347 | #346 | `get_llm_routing` round-trip + `defaults.json` `system_prompt` für 5 Tasks + factory wired auch system_prompt-only Tasks |
| #349 | #348 | Source-Indikator pro Task im LLM-Editor (Cascade-Stufe sichtbar als farbiges Badge) |
| #352 | #351 | Hybrid-Library: `system_prompt_by_provider`-Map + Modal-Default-Vorlage + Save-as |
| #353 | — | JS-Escape-Hotfix (`'\\'` zerlegte das gesamte `<script>`) |
| #354 | — | Per-Provider-Override = `<select>` aus Library + Auto-refresh nach Save |
| #355 | — | Library pre-load vor Editor-Render (Race-Condition-Fix) |
| #356 | — | Variante-Dropdown war leer — ID-typo `ed-pv-` vs `ed-prov-` |
| #360 | #359 | Click-Expand für OWASP/AI-Act-Codes im Audit-Trail + Schranken-Protokoll |
| #358 | — | `.gitignore`: `config/llm/prompts/` ist Operator-Daten, nicht Repo-Code |

### Repo-Cleanup
- 64 alte Branches auf origin gelöscht (alle in main gemerged)
- 14 lokale Branches gelöscht
- `manual/252-compliance-legend` verworfen (Konflikt mit #348/#351 — sauber neu in #359/#360)
- Alte Audit-Logs (`agent_2026-04-*`) → `data/logs/archive/`
- Veraltete Docs → `docs/archive/`
- `__pycache__` / `.pytest_cache` / `.ruff_cache` / `samuel.egg-info` weg

## Aktueller Stand der Issue-Liste

**16 offene Issues** auf Gitea (alle inhaltliche Arbeit, keine Karteileichen):

| # | Titel | Prio |
|---|---|---|
| #357 | refactor(prompts): Konzept konsolidieren (Variante-Dropdown raus, ein Mechanismus) | **User priorisiert** |
| #337 | LLM Retry-Adapter mit Exponential-Backoff (Backlog #133 F8) | low |
| #336 | Event-Sourcing für Workflow-Replay (Backlog #133 F6) | low |
| #335 | Structured Output / JSON-Schema für LLM-Calls (#133 F4) | low |
| #334 | Dead-Letter-Queue für fehlgeschlagene Events (#133 F3) | low |
| #333 | Audit-Log HMAC-Chain (#133 F2) | low |
| #332 | RBAC / Benutzersystem (#133 F1) | low |
| #290 | OWASP Top-10 Migration A01..A10 → ASI01..ASI10 (offiziell 2026) | medium |
| #289 | auto_merge_pr End-to-End Live-Smoke gegen Gitea | medium |
| #270 | LLM-generierter Code absolut kenntlich machen | concept |
| #247 | Plan-Komplexitäts-Wächter Schicht B+C (Scope-Creep + Out-of-Scope-Diff) | medium |
| #230 | SCM-Aktivität ins Bus einspeisen — Activity-Tab | medium |
| #208 | TamperDetected/UnauthorizedChange/IntegrityViolation Events publizieren | medium |
| #153 | LLM-Qualitätskontrolle: gemessene Gewichtung pro (Provider, Modell, Task) | medium |
| #152 | Context-Builder: Skeleton-as-TOC + Slice-Request-Loop (TOC done, SLICE fehlt) | medium |
| #124 | pre-receive Hook auf Gitea-Server installieren (Code fertig, Operator-Aufgabe) | low |

## Aufmerksamkeitspunkte für die nächste Session

### #357 zuerst, BEVOR neue Prompt-Features gebaut werden
Issue-Body hat klare ACs. **Variante-Dropdown raus aus dem Modal**, Per-Provider-Zuweisung **nur** über Task-Editor (`system_prompt_by_provider`-Map). Memory-Eintrag `feedback_prompt_concept_redundant.md` ist die Hintergrund-Notiz.

### Offen aus dem #351-Audit (kleinere F3/F4)
- **F3:** `delete_prompt`-Endpoint sollte idempotent sein (heute 400 bei „nichts zu löschen", korrekt wäre 200)
- **F4:** Modal-Source-Indikator zeigt bei leerer Variante die Cascade-Resolution statt „kein Override an Variante X"

Beides klein, könnte parallel zu #357 oder als eigener kleiner PR kommen.

### Manual-Smoke-Punkt
`manual/252-compliance-legend` und alle anderen alten Branches sind komplett weg. Repo ist sauber: nur `main` lokal + remote.

## Setup-Erinnerungen

- **Tests:** `.venv/bin/python -m pytest tests/ samuel/ -q`
- **Architektur-Tests:** `tests/test_architecture_v2.py` — Slice-Iso. Wenn neue Adapter-Imports im Slice nötig sind: Dependency-Injection im Wiring-Layer (`samuel/server.py`), siehe `_balance_resolver`, `_connection_tester`, `_prompt_source_resolver`.
- **DASHBOARD_HTML:** in `samuel/server.py` als f-string. Bei JS-Edits: **Backslash-Escapes vermeiden** — String-Vergleich auf `\` über `charCodeAt(92)` machen, sonst bricht der Python-f-string das gesamte `<script>` (Bug-Beispiel: `#353`).
- **JS-Parse-Check:** nach Edits an `server.py`:
  ```bash
  python3 -c "import samuel.server as s, re; m=re.search(r'<script>(.*?)</script>', s.DASHBOARD_HTML, re.DOTALL); open('/tmp/dash.js','w').write(m.group(1) if m else '')"
  node --check /tmp/dash.js
  ```
- **Git-Author-Override pro commit:** `git -c user.name=Alexmistrator -c user.email=axlvirtuell@gmail.com commit ...` — niemals `git config`.

## Memory-Eintrag-Übersicht

`MEMORY.md` in `~/.claude/projects/-home-alexanderbenesch-S-A-M-U-E-L-/memory/` enthält jetzt **23 Einträge**. Wichtigste neu hinzugekommene oder erneut bestätigte:

- `feedback_prompt_concept_redundant.md` — zwei parallele Prompt-Mechanismen, bei nächster Optimierung konsolidieren (#357)
- `feedback_dashboard_js_escapes.md` — Backslash-Trap im f-string (durch #353 erneut bestätigt)
- `feedback_self_referential_bug.md` — Inline-Module nach Edits cachen alte Version

## Nächster Schritt — Vorschlag

**Primär:** #357 angehen — Variante-Dropdown raus aus dem Modal, Code-Streichung, ein klarer Workflow. ~4-6h. Bringt UX-Klarheit.

**Alternativ-Quick-Win:** F3 + F4 als kleinerer PR (~1h gesamt). Kosmetisch, aber sauber abräumbar.

**Falls Compliance-Migration drängt:** #290 (OWASP A01..A10 → ASI01..ASI10) — die offizielle 2026-Taxonomie wird ohnehin Pflicht.
