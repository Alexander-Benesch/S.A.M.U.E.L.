# Handover-Doku — Stand 2026-05-05

Diese Datei fasst den Code-Stand und die offenen Themen zusammen, sodass eine neue LLM-Session direkt weiterarbeiten kann, ohne die alte Session-Historie zu lesen.

## Repo-Kontext

- **Working dir:** `/home/alexanderbenesch/S.A.M.U.E.L.`
- **Gitea:** `Alexmistrator/S.A.M.U.E.L` auf `http://192.168.1.60:3001`
- **Gitea-Token:** in `/home/alexanderbenesch/gitea-agent/.env` (`GITEA_TOKEN`)
- **Branch:** `main` (alle PRs werden direkt nach main gemerged)
- **Tests:** `1239 passed, 1 skipped` als Baseline. Befehl: `.venv/bin/python -m pytest tests/ samuel/ -q`
- **Dashboard:** `nohup .venv/bin/python -m samuel --self dashboard --host 0.0.0.0 --port 7777 &` — laeuft auf Port 7777

## Letzte Welle (April-Mai 2026)

In zwei Sessions wurde Phase-1-Hardening + LLM-Editor gebaut. Highlights aus den letzten ca. 30 PRs:

- **#318 OpenRouter-Gateway-Adapter** — unified Billing, 350+ Modelle, Balance-Abruf
- **#314 Test-Connection-Button** + Balance-Anzeige im Editor
- **#315 System-Prompts View + Edit-Modal** (Premium-gated Edit)
- **#316 Schedule-Section** (Tag/Nacht-Switch, Premium llm_routing_advanced)
- **#319 Self-Mode Hard-Stop** + Health-Metrics (jsonl)
- **#328 LMStudio /v1-Suffix-Normalisierung** + validate-Schema-Check
- **#194 Health-Tab** delegiert jetzt an HealthCheckCommand (statt Mini-Dict)
- **#294 Premium-Plugin-Foundation** — Ed25519-Lizenzen
- **#301-#304** LLM-Routing per Task, Time-Window-Schedule, Gemini, OpenAI-direct

Am Session-Ende (2026-05-05 spaet) noch: 6 F-Backlog-Issues aus #133 ausgekoppelt (#332-#337) und #338 fuer Prompt-Haertung + Per-Modell-Overrides angelegt — Material aus User-Walkthrough von `samuel/core/prompts/planner.md`.

## Offene Issues — 19 Stueck (sortiert nach Prio/Aufwand)

### Quick-Wins (1-3h)

| # | Titel | Status |
|---|---|---|
| #124 | pre-receive Hook auf Gitea-Server installieren | Code fertig, **Operator-Aufgabe** |
| #163 | ChangelogCommand braucht Trigger (CLI) | Handler registriert, fehlt CLI-Subcommand `samuel changelog` |
| #289 | auto_merge_pr End-to-End Live-Smoke | Code+Tests done, manueller Live-Test offen |
| #338 | System-Prompts haerten (Schicht A) | 7 Findings aus User-Walkthrough planner.md (Skeleton-Beispiel, AC-Tag-Beispiel, Edge-Cases, Seiteneffekte-Checkliste, Selbstreflexion); auf alle 7 Prompts anwenden |

### Mittel (4-8h)

| # | Titel | Status |
|---|---|---|
| #209 | IVersionControl.get_branch_protection | Komplett offen — Methode + Adapter |
| #208 | TamperDetected/UnauthorizedChange/IntegrityViolation Events | Display-Logic da, Publisher fehlt — am einfachsten via pre-receive-Hook |
| #277 | Multi-Run-History pro Issue + Recovery-Trend | `get_workflow_runs(issue_num)` + Frontend-Section |
| #152 | Context-Builder Skeleton-as-TOC + Slice-Request-Loop | TOC-Mode existiert (`FALLBACK_TOC_THRESHOLD=300`); SLICE-Request fehlt |
| #338 | Per-Modell-Prompt-Overrides + Reset (Schicht B+C) | Erweiterung von #315 — 4-Stufen-Lookup (model→provider→generic→package), Modal mit Variante-Dropdown + Reset-Button |

### Groesser (1+ Tag)

| # | Titel | Status |
|---|---|---|
| #230 | SCM-Aktivitaet ins Bus | Webhook-Erweiterung (`issue-closed`, `pull_request-*`) + Activity-Tab |
| #247 | Plan-Komplexitaets-Waechter Schicht B+C | Schicht A (Komplexitaet) done, B (Scope-Creep) + C (Out-of-Scope-Diff) offen |
| #153 | LLM-Quality pro (Provider, Modell, Task) | Komplett offen — neues Event, Aggregation, Heatmap |
| #270 | LLM-Code kenntlich machen | Komplett offen — Konzept-Issue, viele Optionen |
| #290 | OWASP ASI-Migration (2026 offiziell) | Mapping-Tabelle alt→neu, ~80 Eintraege |

### Backlog (aus #133 aufgespalten, alle frisch erstellt 2026-05-05)

| # | Titel | Phase |
|---|---|---|
| #332 | RBAC / Benutzersystem | Production-Setup-Vorbereitung |
| #333 | Audit-Log HMAC-Chain | Compliance |
| #334 | Dead-Letter-Queue | Concurrency-Phase |
| #335 | Structured Output / JSON-Schema | Code-Quality-Sprung |
| #336 | Event-Sourcing fuer Workflow-Replay | Forensik-Tool |
| #337 | LLM Retry-Adapter | Vor Free-Tier-Provider |

## Sauberkeitsstand

- **Lokale Branches**: nur `main` und `manual/252-compliance-legend` (siehe unten)
- **__pycache__**: alle geloescht
- **Working tree**: clean (alle Aenderungen gemerged)
- **Tests**: 1239 passed, 1 skipped

## Aufmerksamkeits-Punkt: `manual/252-compliance-legend`

Der Branch hat 2 nicht-gemergte Commits:

- `68a856f feat(dashboard): Click-Expand statt Tooltip für OWASP/AI-Act-Beschreibungen (#252)`
- `d325b11 feat(dashboard): Audit-Trail-Tooltips für OWASP/AI-Act-Codes (#252)`

Issue #252 wurde mit einem aelteren Commit (`397ddff`) via PR #268 gemerged — die zwei spaeter gepushten Commits haengen unverbunden auf dem Branch. **Entscheidung der naechsten Session:** entweder als neuen PR mergen oder verwerfen.

## Cleanup-Backlog (optional, destructive)

- **57 origin-Branches** sind alle in main gemerged. Wuerde ein Cleanup mit `git push origin --delete <branch>` bedeuten — destructive auf dem Server. Nicht ohne explizite User-Erlaubnis.

## Memory

`MEMORY.md` in `/home/alexanderbenesch/.claude/projects/-home-alexanderbenesch-S-A-M-U-E-L-/memory/` enthaelt 22 persistente Eintraege (User-Preferences, Project-Facts, Feedback-Patterns). Wichtigste:

- **Self-Mode-Workflow-Pflicht** (`feedback_self_workflow_mandatory.md`) — bei `chat_mode_pr=true` immer `--chat-workflow` nutzen
- **Self-Mode venv-Dependencies** (`feedback_self_mode_venv_deps.md`) — neue pyproject-Deps brauchen `pip install` vor Self-Mode-Start
- **Read vor Edit** (`feedback_read_before_edit.md`) — Read-Tool vor Edit-Tool, `--get-slice` zaehlt nicht
- **JS-Escapes in DASHBOARD_HTML** (`feedback_dashboard_js_escapes.md`) — `\n`/`\t` muessen verdoppelt sein
- **Scope-Creep Disziplin** (`feedback_scope_creep_discipline.md`) — Bugs als separates Issue, nicht im aktuellen mitfixen
- **Bestehenden Code pruefen** (`feedback_check_existing_code.md`) — vor neuer Deklaration grep ob Variable schon existiert

## Bekannte Pflaster / TODO im Code

Stand 2026-05-05 sind keine offenen `TODO`/`FIXME`/`PLACEHOLDER`-Kommentare an Hot-Path-Stellen bekannt. Falls beim naechsten Auftrag etwas ueber den Weg laeuft, das hier zu dokumentieren.

## Setup-Erinnerungen

- **Tests:** `.venv/bin/python -m pytest tests/ samuel/ -q`
- **Type-Check:** keine projektweite Pflicht (kein mypy in CI)
- **Architektur-Tests:** `tests/test_architecture_v2.py` prueft slice-iso (slices duerfen keine adapter importieren) — wenn neue Adapter-Imports im slice noetig sind, muss die Schnittstelle im server.py oder einer Wiring-Schicht sein, oder via Dependency-Injection (siehe `_balance_resolver`, `_connection_tester` in `server.py:1153+`)
- **DASHBOARD_HTML:** in `samuel/server.py` als f-string. Bei JS-Edits: `\n`/`\t`/`\\` immer verdoppeln. Nach Code-Aenderung Dashboard neustarten und im Browser testen
- **Dashboard-Neustart:** `pkill -9 -f "samuel.*dashboard"; find samuel -name __pycache__ -delete; nohup .venv/bin/python -m samuel --self dashboard --host 0.0.0.0 --port 7777 > /tmp/samuel-dashboard.log 2>&1 & disown`

## Naechster Schritt — Vorschlag

**Empfehlung Quick-Win:** #338 Schicht A (Prompt-Haertung) — 3-4h, hilft sofort der Self-Mode-Qualitaet bei lokalen LLMs (qwen-7b, llama3:8b, deepseek-r1), ohne Architektur-Risiko. User-Walkthrough hat das Material schon vorbereitet.

**Alternative Quick-Win:** #163 (ChangelogCommand CLI) — 2-3h, gibt dem Operator ein nuetzliches Tool, kein Risiko fuer existierende Workflows.

**Empfehlung Medium:** #209 (Branch-Protection) — gut umrissen, blockt #208 als Follow-up.

**Empfehlung Big-Picture:** #290 (OWASP ASI-Migration) — die offizielle 2026-Taxonomie wird ohnehin Pflicht; jetzt erledigen waehrend die Mappings noch ueberschaubar sind.

**Wenn Multi-Modell-Setup Prio bekommt:** #338 Schicht B+C (Per-Modell-Prompts mit Reset-to-Default im Dashboard).
