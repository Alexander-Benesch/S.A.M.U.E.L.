services:
  samuel:
    build: .
    ports:
      - "7777:7777"
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
    restart: unless-stopped
    command: ["dashboard"]
